import { IArticlePostBody } from "../../interfaces/IArticlePostBody";
import AddArticle from "./AddArticle";

export default function EditArticle({ title, eventDate, content, categories, tags, section }: IArticlePostBody) {}
